require('dotenv').config();const express=require('express'),cors=require('cors'),mongoose=require('mongoose'),Task=require('./models/Task');const app=express();app.use(cors());app.use(express.json());app.get('/api/tasks',async(_,res)=>res.json(await Task.find().sort({createdAt:-1})));app.post('/api/tasks',async(req,res)=>{try{res.status(201).json(await Task.create({title:req.body.title}))}catch(e){res.status(400).json({message:e.message})}});app.put('/api/tasks/:id',async(req,res)=>{try{const x=await Task.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true});x?res.json(x):res.status(404).json({message:'Task not found'})}catch(e){res.status(400).json({message:e.message})}});app.delete('/api/tasks/:id',async(req,res)=>{const x=await Task.findByIdAndDelete(req.params.id);x?res.json({message:'Deleted'}):res.status(404).json({message:'Task not found'})});app.get('/health',(_,res)=>res.json({ok:true}));mongoose.connect(process.env.MONGO_URI).then(()=>app.listen(process.env.PORT||5000,()=>console.log('Todo API running'))).catch(console.error);