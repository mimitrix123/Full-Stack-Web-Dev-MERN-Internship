import React,{useState}from'react';import{createRoot}from'react-dom/client';import axios from'axios';function App(){const[file,setFile]=useState(null),[url,setUrl]=useState('');return <main><h1>Image Upload</h1><input type="file" accept="image/*" onChange={e=>{setFile(e.target.files[0]);setUrl(e.target.files[0]?URL.createObjectURL(e.target.files[0]):'')}}/>{url&&<img src={url} width="300"/>}<button disabled={!file} onClick={async()=>{const f=new FormData();f.append('image',file);const r=await axios.post('http://localhost:5004/api/upload',f);setUrl(`http://localhost:5004${r.data.url}`)}}>Upload</button></main>}createRoot(document.getElementById('root')).render(<App/>);