import React,{useMemo,useState}from'react';import{createRoot}from'react-dom/client';import'./style.css';
const posts=[{id:1,title:'Learning React Components',category:'React',body:'Build small reusable components and compose them into larger interfaces.'},{id:2,title:'REST APIs with Express',category:'Backend',body:'Design predictable endpoints and validate input at the API boundary.'},{id:3,title:'MongoDB Basics',category:'Database',body:'Model application data and use Mongoose to query MongoDB.'},{id:4,title:'Responsive CSS',category:'Frontend',body:'Use Grid, Flexbox and media queries to make layouts adapt.'}];
function App(){const[q,setQ]=useState(''),[cat,setCat]=useState('All');const cats=['All',...new Set(posts.map(p=>p.category))];const filtered=useMemo(()=>posts.filter(p=>(cat==='All'||p.category===cat)&&`${p.title} ${p.body}`.toLowerCase().includes(q.toLowerCase())),[q,cat]);return <main><header><p>React Blog UI</p><h1>Practical MERN notes</h1><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search posts..."/><div>{cats.map(c=><button key={c} onClick={()=>setCat(c)}>{c}</button>)}</div></header><section className="grid">{filtered.map(p=><article><small>{p.category}</small><h2>{p.title}</h2><p>{p.body}</p></article>)}</section></main>}createRoot(document.getElementById('root')).render(<App/>);