import React,{useState} from 'react';import{createRoot}from'react-dom/client';import'./style.css';
const Header=({title})=><header><h1>{title}</h1></header>;const Footer=()=> <footer>Reusable React Components Practice</footer>;const Card=({title,children})=><article className="card"><h2>{title}</h2><p>{children}</p></article>;const Button=({children,onClick})=><button onClick={onClick}>{children}</button>;function Form({onAdd}){const[v,setV]=useState('');return <form onSubmit={e=>{e.preventDefault();if(v.trim()){onAdd(v.trim());setV('')}}}><input value={v} onChange={e=>setV(e.target.value)} placeholder="Add an item"/><Button>Add</Button></form>}function App(){const[items,setItems]=useState(['Props make components reusable','State makes UIs interactive']);return <><Header title="React Components Practice"/><main><Card title="Header / Card / Button">Reusable components receive data through props.</Card><Form onAdd={x=>setItems([...items,x])}/><ul>{items.map((x,i)=><li key={i}>{x}</li>)}</ul></main><Footer/></>}createRoot(document.getElementById('root')).render(<App/>);