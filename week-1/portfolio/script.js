const menuBtn=document.querySelector('#menuBtn'),nav=document.querySelector('#nav');menuBtn.addEventListener('click',()=>nav.classList.toggle('open'));
document.querySelector('#contactForm').addEventListener('submit',e=>{e.preventDefault();const name=document.querySelector('#name').value.trim(),email=document.querySelector('#email').value.trim(),message=document.querySelector('#message').value.trim(),out=document.querySelector('#formMsg');if(!name||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)||message.length<10){out.textContent='Please enter a valid name, email and a message of at least 10 characters.';return}out.textContent='Thanks! Your message passed validation.';e.target.reset()});