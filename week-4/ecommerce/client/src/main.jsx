import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import axios from'axios';import'./style.css';const API=import.meta.env.VITE_API_URL||'http://localhost:5005/api';function App(){const[products,setProducts]=useState([]),[cart,setCart]=useState(JSON.parse(localStorage.cart||'[]')),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[token,setToken]=useState(localStorage.token||'');useEffect(()=>{axios.get(`${API}/products`).then(r=>setProducts(r.data))},[]);const add=p=>{const next=[...cart,p];setCart(next);localStorage.cart=JSON.stringify(next)};const checkout=async()=>{if(!token)return alert('Login first');const items=Object.values(cart.reduce((a,p)=>{a[p._id]??={product:p._id,name:p.name,price:p.price,quantity:0};a[p._id].quantity++;return a},{}));await axios.post(`${API}/orders`,{items},{headers:{Authorization:`Bearer ${token}`}});setCart([]);localStorage.cart='';alert('Order placed')};return <main><header><h1>MERN Shop</h1><span>Cart: {cart.length}</span></header><section className="auth"><input placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)}/><input placeholder="Password" type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button onClick={async()=>{const r=await axios.post(`${API}/auth/login`,{email,password});localStorage.token=r.data.token;setToken(r.data.token)}}>Login</button></section><section className="grid">{products.map(p=><article><div className="placeholder">Product</div><h2>{p.name}</h2><p>{p.description}</p><strong>₹{p.price}</strong><button onClick={()=>add(p)}>Add to cart</button></article>)}</section><aside><h2>Cart</h2>{cart.map((p,i)=><p key={i}>{p.name} — ₹{p.price}</p>)}<button onClick={checkout} disabled={!cart.length}>Checkout</button></aside></main>}createRoot(document.getElementById('root')).render(<App/>);